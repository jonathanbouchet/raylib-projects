## Motivation
- often times a game or app will require to have an underlying grid system
- this repo is providing a base class to split a game screen (`width`, `height`) according a tile_width (that will be the size of a component of the grid)